# 유형 예측표 (predicted_axes) — 규칙 + 생성 스크립트

45단원 분류표의 각 유형에 대해 「여기서 잘 틀리는 축」을 규칙으로 예측한다.
분류표(어디에 속하는가)와 별개이며, **predicted_ 전용** — 학생 실측(observed_)과
절대 같은 필드에 저장하지 않는다.

## 파일

| 파일 | 내용 |
|---|---|
| `axis_rules.v10.json` | 규칙 299개 (공통 15 + 단원팩 284: …·GE2_CM2GE 41·CA1_M1D 24·SP_CM2SP 35). CA2_M2D 는 M2D+M1D 공용 |
| `axis_rules.v9~v1.json` | 이전 버전. 기록용 보관 — 스크립트는 **v10** 을 읽는다 (v5는 검수 채팅 내부 번호, 리포엔 없음) |
| `build_axis_prediction.py` | 생성 스크립트 **v5** — 행별 폴백(v3) + 유형요약 방출·pack_gap(v4) + 중영역 제거(v5) |

**규칙과 스크립트는 항상 같이 커밋한다.** 스크립트만으로는 재생성이 불가하다.

### v5 변경 (중영역 제거)
- 매칭 컨텍스트 `중영역 + 유형 묶음 + 유형 이름` → **`유형 묶음 + 유형 이름`**.
  45단원 실측: 공통규칙 발화 12,820건 중 이름발 58%뿐, 1,813건이 중영역만으로 걸림.
  중영역은 단원 주제어라 전 항목에 같은 축을 붙여 적중률만 부풀린다(EQ C-11 100%·IN C-12 100%).
  비용 373행이 미매칭으로 내려가나 표본 대부분 오탐. **AELF 는 353·96%·팩76%·gap20% 불변.**
  중영역은 unmatched·pack_gap 시트엔 계속 실어 규칙 설계 재료로 쓴다.
- 규칙 v3: `MM_CMM` 팩 15개(CMM 0%→93%) + C-09 오탐 수정(bare `최대|최소`가 최대공약수·최소공배수를 잡던 것 제거, PF 91건→0).
- 규칙 v4: `PF_M1S1` 팩 16개(소인수분해, D-PF-*). PF_M1S1 149건 100% 회수·팩89%·gap11%·과다없음.
- 규칙 v6: `EI_CMEI` 38개(방정식과부등식) + `SQ_ASQ` 30개(수열). CMEI 540/적중539/팩87%/gap13%, ASQ 459/적중452/팩86%/gap13%. 둘 다 과다규칙 없음.
- 규칙 v7: `TF_ATF` 32개(삼각함수). ATF 385/적중384/팩99%/gap1%/과다없음 — 지금까지 최고 커버리지. D2(공식 선택)가 이 단원 지배축.
- 규칙 v8: `GE2_CM2GE` 41개(도형의 방정식). CM2GE 282/적중282/팩100%/gap0%/과다없음 — 첫 gap·미매칭 0. 「이름단독 약함」은 팩 부재 탓이었고 이름은 정형(이름단독 93%).
- 규칙 v9: `CA1_M1D` 24개(미적분Ⅰ 미분) + `CA2_M2D` 를 M1D 공용으로 확장(applies_to M2D→M2D·M1D). CA2_M2D가 M1D 289건의 51%(147건) 오발화 없이 커버 실측 → CA1_M1D가 나머지. M1D 289/적중289/팩100%/gap0%, **M2D 완전 불변**. 팩 재사용으로 규칙 수 절감 첫 사례.
- 규칙 v10: `SP_CM2SP` 35개(집합과 명제). CM2SP 306/적중297/미매칭9/팩95%/gap2%/과다없음. 미매칭 9는 기본 명제·조건 판단 그룹(팩 미포함). SQ_ASQ 재사용은 why 불일치로 기각.

### 패치 반영 방식 (파일 전송 실패 시)
검수 채팅→Code 탭 파일 전송이 불안정할 때(zip 3회 중 2회 유실), 규칙 블록을 **채팅 본문에
JSON 텍스트로** 받아 반영한다. 절차: v(n) 을 복사→`domain` 끝에 블록 텍스트 삽입→`version` 만 상향.
**재직렬화 금지**(ConvertTo-Json 은 한글 escape·키순서·공백을 바꿔 기존 팩의 바이트동일성을 깬다) —
텍스트 삽입으로 기존 팩을 바이트 보존하고, 삽입 후 common+각 팩을 v(n)과 대조 검증한다.
정규식 `\\(` 는 JSON 문자열에서 `\(` 가 되므로 두 백슬래시 그대로 둔다.
- **기존 팩 수정이 필요한 경우(v9 CA2_M2D)**: 바이트동일 불변식을 그 팩만 완화한다 —
  `applies_to`·`note` 등 지정된 필드만 문자열 치환(치환 매치 수=1 확인), 나머지 팩은 완전 동일,
  수정 팩도 `rules` 배열은 바이트 동일. 다른 단원(예: M2D)의 실행 결과 불변을 재실행으로 확인.

### PF 코드 충돌 처리 (v4 호출 규약)
`PF` 는 소인수분해(M1S1)·다항식곱셈(M3S1) 두 단원에 붙는다. 팩은 prefix 로 선택되므로
호출 키를 **`PF_M1S1` / `PF_M3S1`** 로 갈라야 팩이 안 섞인다(Python 은 CLI 인자, 포팅은
`unit_code=PF → PF_$학기` 매핑). 규칙 `applies_to` 도 `["PF_M1S1"]`. 나머지 44단원은 prefix 유일.

### v4 변경 (누적)
- `유형요약` 시트 → 배지없음 묶음을 묶음 이름으로 1행 방출(45단원 156건).
- **`pack_gap` 시트**: 걸렸으나 단원팩(D-) 못 짚고 공통(C-)만 걸린 항목 = 팩 필요성 실제 지표.

## 실행 (Python + openpyxl) — 정본

```bash
python build_axis_prediction.py <분류표.xlsx> <PREFIX> [출력.xlsx]
```

`axis_rules.v10.json` 은 스크립트와 같은 폴더에 둔다. 출력 시트: `predicted_axes`
· `unmatched` · `pack_gap` · `warnings` · `rule_freq` · `meta`.
PF 두 단원은 prefix 를 `PF_M1S1` / `PF_M3S1` 로 넘긴다(위 「PF 코드 충돌 처리」).

## 정본 / 역할 (안 A + 분업)

- **예측표 공식 생성**: Python 스크립트(검수 채팅). 이 리포지토리의 정본이다.
- **저장·git·결과관리 + 45단원 대량 실행**: Code 탭.
- 이 환경(Code 탭)에는 python3 가 없다. `tools/mathflat_builder/Run-AxisPrediction.ps1`
  이 **v4 를 저장 JSON 에 포팅한 대량 실행기**다 — xlsx 를 매번 검수 채팅에 올리는 것이
  비현실적이라, 45단원 전량은 여기서 돌리고 검수 채팅은 표본으로 교차검증한다.
  두 구현은 **AELF 에서 총353·적중96%·팩76%·gap20%·이름단독92%·요약전용1 로 완전 일치**
  (v2 `detect_layout` 의 단원단위 층선택 버그도 이 교차검증이 잡았다).

  ```powershell
  # 45단원 전량 (또는 -Only M2D 로 한 단원)
  tools\mathflat_builder\Run-AxisPrediction.ps1
  ```

  출력 CSV(임시폴더\axispred): `name_source_dist` · `unmatched_all` · `pack_gap_all` · `rule_over60`.
  `unmatched_all`·`pack_gap_all` 에는 **학기 열**이 있어 PF 코드 충돌(M1S1 소인수분해 /
  M3S1 다항식곱셈)을 분리한다.

## depth 는 행 속성이다 (중요)

한 단원 안에서 행마다 이름 출처가 섞인다 — 예: AELF 353행 중 세부유형 79 · 주제유형
273 · 유형묶음 1. 따라서 `depth` 를 **단원 단위 필드로 굳히지 말 것.**
`index.v1.json` 45단원 교체 시 이 점을 반영한다(단원 하나가 group_only 인 경우는
GEC 처럼 전 행이 유형묶음일 때뿐이다).

## 미결

- **다음 팩(규칙 v11)**: pack_gap 상위 FN 216·BG 193·M2I 181(M1I와 묶어 커버율 선측정).
- **공통 60% 상한**: 중영역 제거 후 QE C-11 90%·EQ C-11 63% 등 8행 남음(묶음 이름이 주제어).
  AELF·CMM 에서 억제되는 규칙이 없어 미구현 — 상한 도입 시 QE/EQ 부터.
- **PSS/PSC 세분화**: D-PS-05 64%·D-PS-09 61%는 삭제 아니라 이산/정규/이항으로 쪼갬(규칙 v4).
  항목 적은 단원(23·56)엔 60% 상한 그대로 적용 금지.
- **PF 코드 충돌**: M1S1(소인수분해)·M3S1(다항식곱셈) 둘 다 PF. 학생 데이터 투입 전 한쪽 변경(§8-B).
- **정확도 검증(배포 게이트)**: 지금까지 잰 건 전부 적중률·커버리지지 예측 축이 맞는지 아님. 근거는 AELF 30문항 88% 하나뿐.
  팩 9개 신규(MM_CMM·PF_M1S1·EI_CMEI·SQ_ASQ·TF_ATF·GE2_CM2GE …) → 단원마다 대표 5~10문항으로 재측정 필요. §17-4 채점·§17-5 증명템플릿의 전제.
- CA2_M2D 확장: **M1D 완료(v9)**. M1LC·M2SL·M2I 는 커버율 선측정 후 (M2I는 M1I와 묶어 볼 것).
- **팩 재사용 절차**: 새 단원 시작 시 기존 팩 커버율 먼저 실측. **채택 기준은 커버율이 아니라 발화 규칙의 `why` 적합성** —
  커버율은 후보 탐색용일 뿐. M1D는 CA2_M2D 9규칙 why 전부 맞아 51% 재사용, CM2SP는 SQ_ASQ 29% 커버였으나
  71건이 범용 `개수`(D-ASQ-21) 하나가 만든 수치라 why 불일치(부분집합 세기≠경계·중복) → 재사용 기각.
