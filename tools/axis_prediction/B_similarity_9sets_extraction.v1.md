# 제출13 — 9세트 1,350문항 추출 완료 · 빈 슬롯 4개 문항 확증 (2026-08-21)

> 수신 검수 · 발신 Code탭 · 판정 15차 §5·§6·§8. **데이터 수정 없음.** 산출 = 추출 CSV 1개.

---

## 0. 세 줄

```
1  9세트 1,350문항 전량 추출. 세트마다 게이트 150/150 통과. 결손 0
2  ★빈 슬롯 6개 중 4개(6·31·36·46)가 문항으로 확증됐다. 제출7·8의 추론이 맞았다
3  ★슬롯 1·3 은 1,350문항 전체에서 미관측. D안 원칙상 만들지 않는다
```

---

## 1. 파일 식별 — 파일명을 믿지 않았습니다 (§7)

폴더에 PDF가 **12개** 있었습니다(요청 대상은 `260711_` 9개, 나머지 3개는 `260710_` 다른 날짜분).

```
앵커 2개  해시가 registry 와 완전 일치
  set04  260711_도형의 닮음(3).pdf  9516aaf0...  = registry.source_sha256
  set09  260711_도형의 닮음(8).pdf  3270f604...  = registry.source_sha256
⇒ 파일명 규칙 확정: set01=접미사 없음 · set02=(1) · … · set09=(8)   (한 칸 밀림)
```

### 해시만 믿지 않고 내용으로도 검증했습니다
```
(8) 을 리포 set09 문항 메타 150건과 대조
  라벨   150 / 150 일치
  정답률 150 / 150 일치
```
set04는 제출12에서 같은 방식으로 이미 150/150 통과했습니다.

### 나머지 7세트의 번호는 ★선언 기준입니다
앵커가 없는 set01·02·03·05·06·07·08은 **index 선언 파일명에 근거한 배정**이라 아직 확정이 아닙니다. 다만 구조적 정황이 그 순서를 뒷받침합니다.
```
세트별 distinct 라벨 수
  set01 76 · set02 75 · set03 70 · set04 67 · set05 65 · set06 62 · set07 57 · set08 55 · set09 51
  ★단조 감소. 예외 0
```
뒤 세트일수록 다루는 유형 종수가 줄어드는 구조이고, **앵커 2개(set04=67 · set09=51)가 그 수열 안에 정확히 제자리**에 놓입니다. 정황 증거이지 확정은 아닙니다 — **D1 배치와의 대조로 확정**하겠습니다(§5).

---

## 2. 추출 — 9세트 전부 게이트 통과

```
set01 150 · set02 150 · set03 150 · set04 150 · set05 150
set06 150 · set07 150 · set08 150 · set09 150        합 1,350
게이트  번호 중복 0 · 1..150 완전 · -ExpectCount 150 일치   (세트마다)
결손 0 · 재시도 0
```
산출: `tools/axis_prediction/B_simpy_pdf_headers_9sets.v1.csv`
```
컬럼  set_declared, source_file, pdf_sha256, question_no, page, accuracy_percent, source_type_label
```
`pdf_sha256`를 행마다 넣었습니다. 나중에 세트 번호가 재배정돼도 **어느 파일에서 나왔는지는 해시로 남습니다.**

---

## 3. ★★빈 슬롯 — 4개가 문항으로 확증됐습니다

9세트 합집합 **76종** = 기존 known 67종 + **신규 9종**.

### 신규 9종이 정렬상 어디에 놓이는가
| mf# | 신규 라벨 | 정렬 슬롯 | 문항수 | 판정 |
|---|---|---|---|---|
| **9** | 두 삼각형이 닮음이기 위하여 필요한 조건 | **6** | 5 | ★**빈 슬롯 확증** |
| **35** | 삼각형의 세 변의 중점을 연결한 삼각형 | **31** | 2 | ★**빈 슬롯 확증** |
| **40** | 삼각형의 무게중심(2) △ABC, △GBC의 무게중심 | **36** | 4 | ★**빈 슬롯 확증** |
| **50** | 닮은 두 입체도형의 겉넓이의 비 | **46** | 3 | ★**빈 슬롯 확증** |
| 3 | 평면도형에서 닮음의 성질 | 2 | 3 | 점유 슬롯(병합 부라벨) |
| 6 | 다면체에서 닮음비의 응용 | 4 | 3 | 점유 슬롯(병합 부라벨) |
| 69 | 변의 길이에 따른 삼각형의 종류 | 56 | 5 | 점유 슬롯(병합 부라벨) |
| 73 | 피타고라스 정리의 활용(3) 직사각형 내부의 점 | 57 | 4 | 점유 슬롯(병합 부라벨) |
| 76 | 피타고라스 나무 문제 | 58 | 6 | 점유 슬롯(병합 부라벨) |

**신규 9종이 정확히 두 부류로 갈립니다.** 4종은 빈 슬롯으로, 5종은 이미 병합으로 정렬된 점유 슬롯으로 떨어집니다. 섞이지 않았습니다.

### 세 방법이 같은 답을 냈습니다
```
제출7  이름 순서 정렬        빈 슬롯 6·31·36·46 의 주인 = mf#9·35·40·50
제출8  문항 라벨 부재        300문항에 그 4종 라벨이 없음
제출10 개념 결번             개념 번호 6·31·36·46 이 결번
제출13 ★1,350문항 라벨 관측   그 4종이 실제로 관측됨. 14문항
```
★ **추론이 문항으로 확증됐습니다.** 판정 15차 §5-③이 요구한 대조입니다.

### 슬롯 1·3 은 미관측입니다
```
슬롯 1  후보 mf#1  닮은 도형에서 서로 대응하는 것    1,350문항 중 0건
슬롯 3  후보 mf#5  입체도형에서 닮음의 성질          1,350문항 중 0건
```
그 자리에서 실제로 나온 것은 **mf#3(평면도형에서 닮음의 성질) 3건**과 **mf#6(다면체에서 닮음비의 응용) 3건**인데, 정렬은 이 둘을 각각 슬롯 2·4에 병합했습니다.

```
판정 요청  슬롯 1·3 처리
  (a) 미관측이므로 생성하지 않는다                      ★Code탭 권고
      D안 원칙 "문항이 요구할 때만" 을 그대로 적용
  (b) mf#3 을 슬롯 1, mf#6 을 슬롯 3 의 주인으로 재배정
      그러면 슬롯 2·4 의 병합이 풀린다. 정렬 결과를 뒤집는 것이라 근거가 더 필요
```
★ 이름 추론(mf#1·mf#5)은 **관측으로 뒷받침되지 않았습니다.** 판정 15차 §5-③의 "안 맞으면 추론을 버리고 관측을 따릅니다"에 해당합니다. 슬롯 1·3의 추론 이름은 **폐기**를 권고합니다.

---

## 4. 신규 라벨의 성격 (§5-④ "기존 base 에 흡수되는가")

```
빈 슬롯행 4종 (mf#9·35·40·50)   흡수되지 않는다. 자기 자리가 비어 있었다
                                 ⇒ D안 (b) 착수 대상. 관측된 칸만 생성
점유 슬롯행 5종 (mf#3·6·69·73·76) 이미 병합 슬롯의 부 라벨로 정렬돼 있었다
                                 ⇒ 새 base 아님. 판정 12차 §3 (c) 배제 방침 그대로
```

---

## 5. 세트 ↔ 배치 대응 (§6) — D1 파일이 있어야 확정됩니다

```
확정   set04 = 260711_(3)  · set09 = 260711_(8)   해시 + 내용 150/150
정황   distinct 라벨 수 단조 감소 수열에 앵커 2개가 제자리
미확정 나머지 7세트의 번호 · D1 배치(simpy-01~08)와의 대응
```
★ 리포 9세트 vs D1 8배치 차이(1세트·150문항)도 이 대조로 갈립니다. **추정으로 짝짓지 않겠습니다.**

---

## 6. difficulty 정정 근거 (§5 후단) — 별건, 실행 안 함

1,350문항 정답률을 검수 4구간(85/70/55)으로 환산하면:
```
basic(>=85)  108
core(70-84)  594
advanced(55-69) 426
high(<55)    222
```
GPT 전사본은 1,200문항이 **전부 core**였습니다. 실제 분포가 이만큼 갈려 있었습니다.
★ **지금 고치지 말라는 지시대로 실행하지 않았습니다.** 정정안은 유형 재배정과 함께 별건으로 제출하겠습니다.

⚠ 단서: 인제스트 산출분의 `difficulty`는 3값(basic/core/advanced)이고 4구간과 값 체계가 다릅니다. 4구간 환산값을 인제스트분에 그대로 덮어쓰면 pos 매핑(제출10 §2)이 깨집니다. **정정 대상은 GPT 전사분(D1)에 한정**해야 합니다.

---

## 7. 남은 것

| # | 항목 | 상태 |
|---|---|---|
| 1 | D1 쿼리 결과 파일(TSV) | 🔴 미수령. 세트↔배치 대조·경로 B 교차검증에 필요 |
| 2 | 슬롯 1·3 판정 (§3) | 검수 판정 요청 |
| 3 | 라벨 ↔ D1 배정 전량 대조 → BASE/CELL 후보 | 파일 수령 후 |
| 4 | 검수 지목 4건(37·62·79/80·63/64) 분류 확인 | 파일 수령 후 |

---

## 8. 측정 재현

```
추출   powershell -File tools\axis_prediction\extract_pdf_type_headers.ps1 `
         -Pdf "<260711_도형의 닮음*.pdf>" -Out <csv> -ExpectCount 150
통합   tools/axis_prediction/B_simpy_pdf_headers_9sets.v1.csv   (1,350행)
앵커   Get-FileHash -Algorithm SHA256 vs registry.source_sha256 (set04·set09)
정렬   tools/axis_prediction/align_catalog_to_mathflat.ps1  (mf# -> slot)
```
