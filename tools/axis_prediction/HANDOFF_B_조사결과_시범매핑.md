# (B) 채점·판정 로직 — 조사 결과 + 4단원 시범 매핑 인계문

> 이 문서는 (B) 착수 조사(`HANDOFF_B_채점로직.md`)에 이은 **데이터 실측 + 오답형 4단원 태그→17축 시범 매핑** 결과다.
> 팩 라인(A)은 45/45 종결(v44). 이 문서는 (B) 전용. 검수측이 축 정의 원문으로 검증할 대상이다("§10~11" 절 표기는 미확인).

---

## ⚠️ 대전제이자 한계 (맨 먼저 읽을 것)

**⚠ 갱신(원문 입수됨): 17축 정의 원문이 `axis_definitions_v10_orig.md`로 리포에 박제됐다.** 아래 이 문서의
축 의미·매핑은 원문이 없던 때 **팩 why에서 복원한 조작정의**에 근거했고, **원문 대조 결과 여러 축이 어긋났다**:
- ✅ 일치: A1·A2·B2·C1·D2·E1·E2·E3
- ❌ 어긋남: **A3**(원문=조건 정리 안 함, 복원=역방향) · **B3**(원문=경우분류 누락·중복, 복원=연산순서) ·
  **C3**(원문=개념 분리·연결) · **C4**(원문=표현 변환 실패, 복원=표기규약) · **D3**(원문=단순 연산 실수, 복원=그래프판독 — 정반대)
- ⚠ 부분: B4(원문=부호 조건)·C2(원문=개념 혼동, 더 넓음)·D1(복원 "계산 슬립" 부정확 — 원문 D1=식 조작)
→ **따라서 아래 §4 시범매핑(86태그 배치)은 원문 기준 재검이 필요하다**(재배치 방향은 `axis_definitions_v10_orig.md` §대조 참조). **재매핑 규모: 6축 657부착 = 전체의 31%**(최대 물량은 복원 목록에서 빠졌던 D1↔D3 교환분).

**⚠ 원문 결손 상태(셋은 서로 다름 — 한 줄로 뭉치지 말 것): `축 정의 내용 확보(출처 절 "§11" 미확인)` · `"§10 축별 공급원 표" 존재 확인 불가` · `"v11 경계 판례" 존재 확인 불가`. ※팩주도율 정의는 별건 확보 — `PACK_HANDOFF.md §16-C`.** "결손 해소"는 축 명칭표 한정. ("없다"가 아니라 "확인 불가" — 원본이 리포 밖·검수측 보유.)

**팩 드리프트 전수대조(「복원≠원문」이 팩 탓인지 내 오독인지):** 「복원≠원문」은 「팩 why≠원문」이 아니라 「내가 팩 why를 잘못 읽음(복원 오독)」일 수도 있음.
- **D1: 1차 판정 = 팩 정상 (잔여 위험 낮음, 재매핑 진행 가능)** — 팩 D1 169규칙 why 전부 식 조작=원문 D1 일치. 역방향 조회(계산슬립 why가 어느 축에 사는가): 전체 1685규칙 중 순수 계산슬립 마커 매치 **8규칙뿐**, B3/D2/C3에 대량 오배정 없음 → **숨은 계산슬립 물량 없음, D1/D3 경계 지켜짐.** (regex 정황증거·폭 한계 있음, 확정 승격은 검수 판단.)
- **D3: 부분 드리프트 → ✅ 처리 완료** (v44, HEAD `0ef59c4e` 기반). 17규칙 중 10 원문부합(단순연산)·7 드리프트(그래프/좌표 판독→식; ST-14·IR-24·M2SL-07·M1LC-15·CG-13/21/26). **⚠정정: "5개 이미 C4 페어"는 틀림 — 실측 3개**(ST-14·M2SL-07·M1LC-15). 처리: **3개 D3 제거 · 4개(IR-24·CG-13·CG-21·CG-26) D3→C4 교체**(4개는 C4 부재라 제거 아닌 교체). D3 축 17→10. **45단원 재검증 통과**(AELF 353/96/76/20/60/1 불변 · 전체 9028·미121·gap687 불변 · 영향 5단원 커버 회귀 0).
- **A3: 팩 정상 (복원 오독) — 오류절 재분류로 확정.** 71규칙 오류절 전수가 A3(조건 정리 안 함 = 무엇이 주어지고 무엇을 묻는지·주어진 게 조건인지 답인지 정리 못함)에 수렴. "역산"은 **해법절**이라 축 안 붙음(내가 해법절로 분류한 방법 오류). 드리프트 0·축공백 0·**역산 축 신설 불필요.**
- **⚠ 방법론 고정: 축은 오류절(대시 뒤)에 붙지 해법절에 안 붙는다.** D3는 오류절 대조라 견고·D1은 해법절이었으나 어느 절이든 식조작이라 생존. **B3(346)·B4(163)·C3(400)은 반드시 오류절 기준**으로(안 그러면 909규칙 흔들림).
- **B3·B4·C3: 오류절 기준 대조 대기.**

**45/45 검증 범위 기록:** 오탐 대장 10건은 전부 **앵커 문자열 포함관계 오탐**(최대/최소⊂최대공약수, 수직⊂수직선 등), **축 의미 오배정은 0건.** D3 드리프트는 45/45가 *놓친* 게 아니라 *안 본 차원* — 검증 무효는 아니나 **커버 범위가 앵커 발화까지**였음을 명시.

### 팩 why에서 복원한 축 조작정의
| 축 | 의미 |
|---|---|
| A1 | 뜻/경향 안 읽고 직관·계산부터 (상관·표준편차 직관비교) |
| A2 | 정의·공식 암기 재생 (평균정의·편차·분산순서·지수법칙·유한소수 판정조건) |
| A3 | 역방향·구성적 (정의를 식으로 세워 거꾸로 복원, 주어진 해에 맞추기) |
| B1 | 상황의 관계·조건을 빠짐없이 식에 반영 (다리+기차길이·구간조건·계수≠0) |
| B2 | 범위·경계 (자연수/정수 조건·끝값포함·양수조건) |
| B3 | 연산 순서·부호 규칙 처리 (곱÷차례·괄호앞부호) |
| B4 | 식 변형 방향 (÷→역수·부등호 뒤집기·거스름돈 방향) |
| C1 | 활용 문장을 식으로 착수 (미지수/상수 정하기·모델링) |
| C2 | 용어·계수·차수 정확히 읽기 (동류항 정의·계수/상수 구분) |
| C3 | 규칙·정의 절차대로 / 두 단계 되돌림 (먼저 간단히 후 대입) |
| C4 | 표기·형식·단위 규약 (문자표기·표현통일·단위맞춤) |
| D1 | 계산 실행 슬립 (소거·정리·나머지·부호계산) |
| D2 | 공식 선택·적용 (거리=속력×시간·a%=a/100·지수법칙·순환소수변환) |
| D3 | 그래프·그림에서 자료 읽어 옮기기 (산점도 축·점·도수) |
| E1 | 되돌려 검토/대입 검산 (원식 대입 확인·되돌림) |
| E2 | 결과 타당성 범위 (표준편차²=분산·음수분산 불가) — **4단원 태그 미사용** |
| E3 | 마무리 검증: 단위·무엇을 묻는지 확인 |

---

## 1. `source_item_links` 발견 (실측)

- **3,364문항 · 14단원**(중2·중3만). `likely_error_tags` **100% 채워짐**(평균 3.57개/문항, 최소1 최대11).
- 필드: item_id · unit_id · primary_problem_type_id · **likely_error_tags** · concept_ids ·
  expected_process_stages · first_action_hint · representation_type · condition_count · mapping_confidence · review_status.
- **⚠ 문제/풀이 text·답·선택지 필드가 저장 안 됨** — policy: *"source text is not stored"*.
  → **답별 오답 분기(진짜 오답예측표)는 이 데이터만으로 불가.** 태그 뜻 확인은 first_action_hint·process_stages·동시태그로 수행.
- 단원별 문항수: 삼각비372·원300·문자와식(NE)300·확률300·이차방정식300·이차함수300·닮음300·다항식(PM)300·
  일차방정식(연립)192·실수150·통계150·기하150·일차부등식150·일차함수100.
- **중1·고등 ~31단원은 문항 데이터 0** — source_item_links에 14단원만. 나머지는 옛 item_bank/algebra만 있고 새로 구축해야 함.

## 2. 태그 어휘 = 단원 전용 (전역 매핑표 불가)

- 14단원 **태그 총 398종**. 그중 **381종(96%)이 단 한 단원에만** 등장(전용). **2단원 이상 공유는 17종(4%)뿐**, 최대 3단원.
- → **전역 단일 "태그→17축" 매핑표는 안 통한다.** 매핑은 **단원 단위**로 짜거나 태그를 상위 개념으로 정규화하는 층이 선행돼야 함.
- 널리 쓰이는 공유 태그: parallel_lines(3단원·241) · verification_missing(3단원·145) · sign_error(3단원·84) · visual_interpretation(2단원·546) 등.
- 상세: `B_tag_unitspread_itemcount.tsv`(398행 = 태그·단원수·문항수).

## 3. 오답형 vs 주제형 분리 (핵심 지형)

태그가 **두 저작 철학**으로 갈림. 부착 기준 오답형%(접미 `error|missing|confusion`)가 **깨끗한 이분(bimodal)** — 단원이 ~0% 아니면 ~78–100%:

| 성격 | 단원수·문항 | 단원 | 판정 |
|---|---|---|---|
| **오답형** | **4단원 · 792문항** | m3_statistics(100%) · m2_linear_inequality(93%) · m2_linear_equation(90%) · m2_number_expression(78%) | "학생이 뭘 틀렸나" → **17축 매핑 직결** |
| **주제형** | **10단원 · 2,572문항** | 삼각비·원·확률·이차함수·이차방정식·닮음·다항식·일차함수·실수·기하 (전부 ≤9%) | "이 문제는 뭐에 관한 것" → 진단정보 희박 |

- **판정 기준 = 오답형 부착비율 ≥ 50%**(엄격·확장 동일 결과, 검산계열이 이미 verification_missing의 missing으로 잡힘).
- **⚠ 주제형 지배태그 100% = 진단정보 0:** m3_circle_properties는 `circle_geometry`+`visual_interpretation`이 **300/300 전량**, m3_trigonometric_ratio는 `trigonometric_ratio`가 **372/372 전량**. 그 태그만으로 진단가치 0.
  (근접: m2_geometry_properties `angle_relation` 91%.)
- **함의:** 주제형 10단원은 태그만으론 거친 진단이 안 됨 → **주제태그→오답 변환**이 필요하고 그건 문항 답/풀이를 읽는 작업(=답별 오답표)인데 §1대로 **답 필드가 없어 막힘**. 4:10 분리가 (B) 로드맵의 상위 제약.

## 4. 오답형 4단원 시범 매핑 (86종 → 17축)

전체 표: `B_trial_mapping_4units_v1.md`. 근거 = 팩 why(축의미) + 문항 메타(태그뜻, 문제text는 미저장).

- **86종**(단순합 90 − 단원걸침 중복 4). **16축 사용 / 1축 공백 = E2**(결과 타당성 범위, 오답형 단원에도 태그 없음).
- **축 없는 태그 = 1종·118문항: `multi_condition_overload`**(수와식) — 오류유형이 아니라 "조건 많아 부하"라는 **난이도 서술자**.
- **다축 태그:** 명확히 2축 = 6종(`unit_conversion_error`=C4+E3, 거리·상대속력·농도 관계식=C1+D2, coefficient_ratio=C2+D2). 기준을 넓히면 ~15종 — 활용 관계식 태그는 본질이 모델링(C1)→공식(D2)→계산(D1) 다단계라 단일축 강제는 정보손실(=답별 분기 필요성의 방증).

### ⭐ 검산 145문항 → E1 착지 (원문으로 확정)
- **원문 축 명칭표: `E1 = 검산 생략`.** → `verification_missing`(3단원 **145문항**: LE93·NE51·LI1)·`solution_check`·`solution_set_omission`은 **E1에 착지 확정.** `HANDOFF_B §4`의 "검산이 17축에 집이 없다"는 **틀렸음이 원문으로 확인됨**(집이 있고, 이름은 E1).
- **⚠ 남는 것은 "축의 참/거짓"이 아니라 "공통규칙 C-01의 E1 부여가 노이즈였나".** 원문상 E1은 **공통 부여 8축 중 하나**(C-01 「주어진 경우」 계열이 공급). DA에선 C-01 공급 34%인데 팩이 E1을 회피 → **E1축은 참이되, C-01의 광폭 E1 부여가 오탐 의심**(오탐 대장 C-01 행과 동일 사안, 「유형② 후보」로 이미 등재). 즉 유령 의심의 대상은 **축 E1이 아니라 공통규칙 C-01**.
- (B) 남은 판단: 검산은 E1로 확정하되, **C-01의 E1 부여 정밀화**(발화율 포화·함정 일치로 오탐 걸러내기)가 별개 과제.

### ⧖ (판정 보류) LI의 A3 = 팩 미예측 축
- 앞서 "철회"로 닫았으나 **성급했음 → "판정 보류"로 되돌림**(여섯 글자 레이블 하나로 판정한 것). **세 질문 중 닫힌 건 ① 하나.**
- A3 semantic 자체는 해소됨(팩 A3=조건 정리 안 함, 오류절 기준 정상). 남은 건 **LI 태그 `solution_set_matching_error`의 오류가 A3(조건 정리)인지 B3(경우 나누기)인지** — 태그엔 오류절이 없고 문제 text 미저장이라 **86태그 재매핑에서 오류절 기준으로 판정.** 그때까지 보류.
- 대신 **새 경계 발생: A3(조건 정리 안 함) vs B1(조건 누락)** — 기존 4경계(B1/C1·C3/C4·E1/E3·A2/D2)에 없던 것. B1 110 + 조건계열 태그가 여기서 갈림(재매핑 시 판정).

### 팩 예측축 vs 태그 실측축 대조
팩 축집합(v44 D-규칙): EX_LE·LI_IN = {B1,B2,B3,B4,C1,C2,C3,C4,D1,D2,E1,E3}(12) · SD_ST = {A1,A2,A3,B1,B2,B3,C2,C3,C4,D1,D2,D3,E1,E2}(14) · NS_NE = {A2,A3,B1,B2,B3,B4,C1,C2,C3,C4,D1,D2,D3,E1,E3}(15).

| 단원 | 겹침 | 팩만(태그 미확인) | 태그만(팩 미예측) |
|---|---|---|---|
| LE | B1·C1·C2·C4·D1·D2·E1·E3 (8) | B2·B3·B4·C3 (4) | 없음 |
| LI | B1·B2·B4·C1·C4·D1·D2·E1·E3 (9) | B3·C2·C3 (3) | 보류 (A3 판정 remap) |
| ST | A1·A2·D1·D2·D3 (5) | A3·B1·B2·B3·C2·C3·C4·E1·E2 (9) | 없음 |
| NE | A2·B1·B2·B3·B4·C2·C3·C4·D1·D2·E1 (11) | A3·C1·D3·E3 (4) | 없음 |

- **태그의 팩 부분집합 여부는 LI A3 판정에 종속(보류)** — solution_set_matching이 A3면 태그만 축 1(팩 미예측), B3면 0. 재매핑에서 확정.
- **⚠ 이 부분집합 관계를 "팩 과다예측"으로 읽으면 안 된다 — 지금 데이터로는 「팩 과다예측」과 「태그 어휘 부족」을 구분할 수 없다. 판정 보류.**
  근거: 통계 태그 14종은 **전부 계산·판독 계열**(A1·A2·D1·D2·D3)이라 **애초에 B·C·E를 표현할 태그 자체가 없다.** 즉 팩이 예측한 B/C/E(예: A3 역산)를 태그가 "확인 안 해준" 게 팩이 틀려서인지, 태그 어휘가 좁아 그 실수를 담을 칸이 없어서인지 **이 데이터만으론 판별 불가.** (통계 태그 5축 vs 팩 14축이라는 대비도 같은 이유로 "팩 과다"의 증거가 못 됨.)
- **→ predicted vs observed 정합/과다 판정은 축 정의 원문 + 태그 어휘의 완전성 점검이 있어야 가능.** (E2도 팩만 예측: SD_ST D-ST-38 — 마찬가지로 태그에 E2 칸이 없어서일 수 있음.)

## 5. 다음 할 일

1. **B3·B4·C3 오류절 기준 대조**(A3·D1·D3 완료) → 그다음 **원문 기준 86태그 재매핑**. ① E1=검산 확정(ⓒ) ② LI A3 판정 보류(remap서) ③ A3 오류절로 해소, A2/D2·B1/C1은 경계 판례 대기(존재 확인 불가).
2. 팩 수정은 **D3 7규칙 드리프트에 한해 A라인 결정 상신**(Code탭 ruling 아님, sha256 재검증 동반).
3. **별도 항목(C층 설계): predicted≠observed 최대 격차** = 팩 D3 10규칙(0.6%, 계산실수 거의 예측 안 함) vs 태그 D3 ≈384부착(원문 기준 재매핑 후 태그 최대 축). 매핑 오류 아니라 **두 저작이 진단 대상을 다르게 잡음** — C층 설계 직결.
4. **축 명칭표(정의문 아님, 17축 한 줄씩·포함/배제 문장 없음).** 레이블만으로 해소된 건 **E1(검산 생략)** 뿐, A3는 오류절 코퍼스로 해소, **A2/D2·B1/C1은 미해소** → 결손 우선순위 **"§10 공급원 표"·"v11 경계 판례"(둘 다 존재 확인 불가) 상향.** "축 명칭표로 최대결손 해소"는 과한 기록.
3. 주제형 10단원: 답 필드 부재 → 문항 답/풀이 원천 확보 여부부터 판단(없으면 거친 진단 불가).
4. predicted_axes 배선(팩→유형, 기계적)은 축 어휘 확정 후.

## 6. 파일 (이 조사 산출물)
```
이 문서                tools/axis_prediction/HANDOFF_B_조사결과_시범매핑.md
86태그 시범매핑 정본    tools/axis_prediction/B_trial_mapping_4units_v1.md
태그 단원분포·문항수    tools/axis_prediction/B_tag_unitspread_itemcount.tsv  (398행)
착수 조사(선행)         tools/axis_prediction/HANDOFF_B_채점로직.md
A라인 정본             tools/axis_prediction/PACK_HANDOFF.md
문항+태그 데이터        public/math-weakness-engine/data/source_item_links/  (14단원 3364문항)
팩 규칙               tools/axis_prediction/axis_rules.v44.json
```

## 7. 손대지 말 것 (§9 불변)
- `index.v1.json`·진단워커·옛 12분할: 완성 후 한 번에 원자 교체. 지금 손대지 말 것.
- `public/`는 GitHub Pages 서빙 — 학생 PII 금지.
- predicted_axes ≠ observed_axes 같은 필드 저장 금지.
