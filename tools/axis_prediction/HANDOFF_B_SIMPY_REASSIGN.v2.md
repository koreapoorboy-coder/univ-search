# Code탭 인계 — 닮음 재배정 트랙 v2 (2026-08-21)

> **새 세션은 이 문서로 진입한다. §0 → §1 → §2 → §7 순으로 읽으면 된다.**
> 선행 문서 `HANDOFF_B_CATALOG_TRUNCATION.v1.md`는 **절단 조사 단계의 기록**이고, 그 단계는 종결됐다. 배경이 필요할 때만 본다.
> 상위: `REVIEW_HANDOFF_matching_phase.v2.md`(§7 실무 함정) · `HANDOFF_검수_20260811.md`(역할·URL만).

---

## 0. 지금 국면 한 줄

```
카탈로그 절단 조사가 끝나고, 지금은 닮음 1,200문항의 유형 재배정 트랙이다.
학습지 PDF 가 문항마다 유형명을 인쇄하고 있다는 것이 밝혀져(경로 A),
재배정이 추론이 아니라 대조 문제가 됐다.
```

**진행 상태**: 대조 1회 완료(제출14) → 좌표 다툼 종결(제출16) → **판정 18차 §3 에서 도구·1,200건 결과 채택**.
지금은 막힌 것이 없다. 빈 슬롯 생성 목록 작성이 다음 착수분이다(§7).
**데이터 수정**: 이 트랙 전체에서 **승인분 3건뿐**.
```
c4596f28  index.v1.json dangling 27건 삭제 (커밋 A)
971056a9  닮음 카탈로그 type_name_source 표식 87/87
(제출18)  닮음 카탈로그 빈 슬롯 4엔트리 생성 87 -> 91 (판정 20차 승인)
          ★백업 _backup/...pre-add-4entries · 기존 87종 무변경(역산 검증)
(제출19)  축약본 87 -> 91 재생성 (판정 21차 §5)
(제출20)  representation_types 오염 제거 66엔트리 528원소 (판정 21차 §7)
          ★백업 _backup/...pre-strip-representation_types · 역산 검증 통과
          ★CHECK 9 로 검출 -> 수정 -> CHECK 9 재실행 0 확인 순서 준수
```
★**역산 검증(reversal check) = 상설 항목**(판정 21차 §1·§9).
데이터 파일에 삽입·치환을 했으면, 그것을 도로 되돌린 결과가 원본과 **바이트 동일**한지 확인한다.
"다른 데 안 건드렸다"를 증명하는 가장 강한 형태다. `add_catalog_entries.ps1` ·
`strip_ps_member_contamination.ps1` 이 내장하고 있고, 실패하면 기록하지 않는다.

---

## 1. 확정된 사실 (재론하지 말 것)

### 1-1. 정식 종수 177 — 네 경로로 확정
```
① docs 선언    patch209/214 validation : canonical_problem_type_count 177 (상한 고정)
② manifest 회계 +519 = 177+126+108+108, 13042-12788 = 254 = 절단 결손 (상한 고정)
③ 개념 증인    카탈로그 참조 개념 max C059 -> 59 base            (하한 증언)
④ 슬롯 산술    59 base × 3칸                                    (하한 증언)
```
★ **개념 증인은 하한만 말한다**(제출11 §1). 상한은 ①②가 고정한다. 통계처럼 조립이 뒤쪽까지 안 닿은 단원에서는 ③이 미달해도 반증이 아니다.

### 1-2. 슬롯 구조
```
정식 177 = 59 base × 3칸.  slot n -> PT(3n-2)/PT(3n-1)/PT(3n)
★pos 표준 = (PT − 1) mod 3  (판정 20차 §3 확정. PT mod 3 로 쓰지 말 것)
칸(pos) ↔ 접미사 1:1, 예외 0
  pos0 = 첫칸 = PT(3n-2) = 개념·조건 판별
  pos1 = 둘째칸 = PT(3n-1) = 비례식·정리 적용
  pos2 = 셋째칸 = PT(3n)  = 복합 계산·증명
  ★현행 표준 목록 = B_simpy_catalog_pos_standard.v1.csv (91행)
  ★HANDOFF_B_CATALOG_TRUNCATION.v1.md 41·277행은 PT mod 3 체계다(정정 판정 대기)
type_name_source 값 = variant_bank | worksheet_label
  ★정의는 ISSUE_type_name_source_values.v1.md. 장식이 아니라 라벨→슬롯 맵을 움직인다
칸 ↔ difficulty 1:1 (인제스트 산출분)  basic/core/advanced, 300/300 예외 0
  ★GPT 전사분(D1)에는 적용 금지. 4값 체계이고 명세가 정렬을 금지한다
슬롯번호 = 개념번호, 91/91 예외 0
현재 카탈로그 = 57 base 점유(91 엔트리) · 빈 슬롯 2개(1·3)
  ★2026-08-21 판정 20차 승인으로 6·31·36·46 생성 완료(제출18). 87 -> 91
```

### 1-3. 빈 슬롯 6개의 현재 판정
| 슬롯 | 주인 | 문항 관측 | 상태 |
|---|---|---|---|
| 6 | mf#9 두 삼각형이 닮음이기 위하여 필요한 조건 | 5건 | **✅생성됨 PT016 pos0** (근거 강) |
| 31 | mf#35 삼각형의 세 변의 중점을 연결한 삼각형 | 2건 | **✅생성됨 PT092 pos1** (★근거 약: 단일 PT 관측) |
| 36 | mf#40 삼각형의 무게중심(2) △ABC, △GBC의 무게중심 | 4건 | **✅생성됨 PT108 pos2** (★근거 약: 단일 PT 관측) |
| 46 | mf#50 닮은 두 입체도형의 겉넓이의 비 | 3건 | **✅생성됨 PT137 pos1** (근거 강) |
| **3** | mf#5 입체도형에서 닮음의 성질 | **4건(07-10 판본)** | **확증. 폐기 권고 철회됨(제출15 §5)** |
| 1 | mf#1 닮은 도형에서 서로 대응하는 것 | **1,800문항 중 0건** | 미출현. 생성하지 않음 |
★ 슬롯 1은 "없다"가 아니라 **"정식에 존재하나 학습지 미출현"**으로 기록한다(판정 16차 §2-④).
★ 다만 `항상 닮은 도형`(mf#2)이 가장 이른 학습지 1·2번에 있어 **슬롯 1/2 경계는 판정 대기**.

### 1-4. 경로 A — 학습지 PDF 가 원본 라벨을 갖고 있다
```
PDF 는 문항마다 "| <유형명> | 정답률 NN%" 를 인쇄한다
pdftotext 로 추출 가능 (pdftoppm 은 없어서 Read 도구의 PDF 읽기는 실패한다)
set04 검증  라벨 150/150 · 정답률 150/150 축자 일치
9세트 1,350문항 + 07-10 판본 450문항 = 1,800문항 추출 완료
```

### 1-5. 세트 ↔ D1 배치 대응 (8/8 확정 · 5경로)
```
simpy-01..08  =  set01..08     set09 은 D1 미투입(인제스트로만 들어감)
근거  범주 슬롯 일치율 대각선 8/8 (마진 1.17~1.80)
검증  라벨 37칸 회전본 -> 0.007~0 -> 0/8 채택
앵커  리포 인제스트 set04 ↔ simpy-04 최고 일치 0.580 (번호 규칙 미가정)
④    전사본 본문 -> 세트 식별 (제출16 §2). D1 problem_type_id 미경유
⑤    전사본 source_note 완전일치 -> D1 배치 (제출16 §3). 150/150 x5. 점수 아님
```

### 1-6. ★전사본 batch_id 는 D1 배치보다 1 낮다 (제출16)
```
전사본 파일 simpy-N  =  provenance.source 닮음(N).pdf  =  set(N+1)  =  D1 batch simpy-(N+1)
틀린 필드는 전사본 안의 bulk_batch_id 하나뿐. source·내용·D1번호·세트번호는 전부 정합
D1 은 무오염 (8x150=1200 · 결번 0 · 잘못된 ID 로 투입된 배치 없음)
```
★ 전사본을 좌표로 쓰지 말 것. **좌표의 정본은 D1** 이다.
★ `SCSTUDY-2026-08-18-m2-simpy-0`(폴더 닮음2) = bulk_batch_id 잘림 6회째.
★ 전사본 simpy-08(=set09 내용)은 105/150 미완결이라 D1 에 안 들어갔다 — set09 미투입의 실제 이유.

---

## 2. 검수 판정 대기 (막힌 것 아님)

### 2-1. ✅종결: 지목 4건의 배치 좌표 (판정 18차 §0 · 제출16)
```
원인   검수 보유 전사본의 bulk_batch_id 가 실제 투입 배치보다 1 낮았다.
       라벨 simpy-05 · 출처 닮음(5) · 내용 set06 · 실투입 D1 simpy-06.
       검수는 set06 본문을 D1 simpy-05 배정과 맞췄다 = 서로 다른 학습지 대조.
범위   보유본 1건이 아니라 전사본 7배치 전부 (제출16 §1). §1-6 참조
결과   판정 8차 §2 · 판정 17차 §2 무효화(판정 18차 §1). 대응은 무사.
       1,200건 대조 결과 채택됨(판정 18차 §3).
```

### 2-2. 학습지 라벨 vs 풀이법 — 무엇이 정답인가 (제출15 §4)
```
simpy-06 q62  학습지 = 중점연결 삼등분선(슬롯30) / 전사·배정 = 직각삼각형 닮음(슬롯12)
문제에 삼등분점이 나와 출판사가 삼등분선 블록에 넣었고, 풀이는 직각삼각형 닮음이다
⇒ 학습지 라벨은 '출판사 배치 기준 분류'이며 풀이법과 다를 수 있다
Code탭 권고 = 학습지 라벨(A). canonical taxonomy 가 그 라벨로 만들어졌으므로 일관됨
★BASE 103건 안에 이런 건이 몇 개인지는 본문 없이 셀 수 없다. 검수가 본문 보유
```

### 2-3. 그 밖의 대기
```
슬롯 1/2 경계     판정 18차 §6 = 현상 유지 확정. mf#2 는 슬롯 2 병합. 추론 금지
BASE 103건 후보 판정   목록 송부 완료(B_simpy_base_candidates_103.v1.csv). 검수 판정 대기
07-10 450문항        라벨 조사 포함 승인 · 배정 대조 미포함 확정(판정 18차 §7)
                     투입 여부는 사용자 판단. distinct 79종 목록 송부 완료
```

---

## 3. 대조 결과 (판정 18차 §3 채택 → 슬롯 생성 후 v2, 제출18)

```
생성 전  OK 718 · CELL 333 · BASE 103 · NEWLABEL 35 · NOID 11   라벨->슬롯 67
생성 후  OK 718 · CELL 346 · BASE 104 · NEWLABEL 21 · NOID 11   라벨->슬롯 71
차이     바뀐 행 정확히 14행. 나머지 468행 무변경. ★기존 판정이 뒤집힌 행 0건
```
- **검수 질문의 답**: base 충돌 · 칸 충돌 둘 다이되 칸 충돌이 3배.
- **BASE 는 무작위가 아니다**: ① 53↔57 대칭 스왑(11+11) ② 45→47→48→50 연쇄 밀림.
- ★**②의 "빈 슬롯 46 때문"이라는 가설은 기각됐다**(제출18 §3-①). 46 생성 후에도 45→47(8) ·
  47→48(9) · 49→50(6)이 전부 그대로다. 애초에 그 행들의 슬롯쌍에 46이 등장하지 않아
  **구조적으로 이 조치와 무관한 현상**이다. 새 가설이 필요하다.
- **NEWLABEL 14건 흡수 완료** → CELL 13 · BASE 1. ★OK 0건인 것이 정상(자리를 만든 것이지
  D1 배정을 고친 게 아니다. 재배정은 §7).
- **신규 BASE 1건** simpy-02 q092 (44→46, 평면 vs 입체). 생성이 깬 것이 아니라 NEWLABEL 통에
  숨어 있던 오배정이 드러난 것.
- **NOID 11건** = 유형 배정 자체가 없는 문항. simpy-06 q25/92/126/148 · simpy-07 q72/94/126/148/149/150 · simpy-08 q75.

---

## 4. 도구 (전부 ASCII 전용 · 3종 시험 통과)

```
check_catalog_integrity.ps1        카탈로그/매니페스트 무결성. CHECK 1~8
                                   7=개념 증인(초과FAIL/일치ok/미달under) 8=회계 대조
extract_pdf_type_headers.ps1       학습지 PDF -> question_no·정답률·유형명
                                   ★-ExpectCount 게이트 필수. 부분 추출을 성공으로 쓰지 않는다
align_catalog_to_mathflat.ps1      슬롯 <-> mathflat 순서 정렬(단조 DP). -EmitSlotFamily
identify_batch_to_set.ps1          D1 배치 <-> 학습지 세트 대응. 범주 일치율로 판정
identify_transcript_to_set.ps1     전사본 내용 -> 세트 식별. D1 미경유(독립 경로)
                                   ★9세트 전량 채점이라 회전 통제가 내장. 마진 게이트 1.05
compare_pdf_label_to_assignment.ps1 학습지 라벨 vs 배정 대조. BASE/CELL/NEWLABEL/NOID
                                   ★OK share 40% 미만이면 "대응이 틀렸다" 가드 발화
                                   ★라벨->슬롯 맵은 인제스트 문항은행(set04/09)에서 만들어진다.
                                     카탈로그를 보지 않는다. 앞머리 세트 전용 유형은 엔트리를
                                     만들어도 NEWLABEL 로 남는다 -> worksheet_label 시드 블록이
                                     그 구멍을 메운다(판정 20차 이후)
add_catalog_entries.ps1            카탈로그 엔트리 텍스트 서지컬 삽입. 전량 재직렬화 금지 대응
                                   ★검증 5항(파싱/엔트리수/필드구성/U+FFFD/★역산) 실패 시 미기록
                                   ★역산 = 삽입분을 도로 들어내면 원본과 바이트 동일한가
match_d1_hints_to_base.ps1         경로 B(힌트 유사도). 경로 A 승격 후 검증 보조로 격하
prune_dangling_index.ps1           index dangling 키 삭제(커밋 A에 사용). blast-radius 가드
mark_type_name_source.ps1          problem_types 에 type_name_source 스탬프
```

### 실행은 반드시
```
powershell -File tools\axis_prediction\<도구>.ps1 ...
```
경로 없이 파이프로 실행하면 ASCII 자기검사가 건너뛰어진다.

---

## 5. 산출 데이터

```
B_simpy_pdf_headers_9sets.v1.csv        1,350행  9세트 학습지 라벨·정답률 (pdf_sha256 포함)
B_simpy_pdf_headers_0710_probe.v1.csv     450행  07-10 판본 3개. ★조사용, 리포 등재 아님
B_simpy_batch_set_pairing.v1.csv            8행  배치<->세트 대응
B_simpy_assignment_candidates.v1.csv      482행  BASE/CELL/NEWLABEL/NOID 후보
B_simpy_missing_cells_72.v1.csv            72행  결손 접미사 칸. basis=mechanical_derivation
B_simpy_slot_family.v1.csv                 59행  슬롯 -> 계열(mathflat 중영역)
B_simpy_set04_pdf_headers.v1.csv          150행  set04 단독(제출12 시험분)
B_simpy_set06_labels_for_review.v1.csv    150행  set06 라벨(판정 18차 §4 요구분, 송부완료)
B_simpy_0710_distinct_labels.v1.csv        79행  07-10 distinct 라벨. 전용 3종 표식
B_simpy_base_candidates_103.v1.csv        103행  BASE 후보(생성 전). 좌표=batch_id+question_no
B_simpy_assignment_candidates.v2.csv      482행  ★생성 후 재대조 후보 (현행)
B_simpy_base_candidates_104.v2.csv        104행  ★생성 후 BASE 후보 (현행)
B_simpy_compare_report.v2.txt                    재대조 리포트 원문
B_simpy_catalog_pos_standard.v1.csv        91행  ★pos 표준 목록. 제출12 별첨의 갱신본
B_simpy_new_slot_entries_4.v1.csv           4행  생성된 엔트리 + 근거 강도
B_simpy_new_slot_requiring_items_14.v1.csv 14행  각 칸을 요구한 문항 좌표
```
★ **원본 1,200행은 검수에 넘기지 않는다**(판정 13차 §5-⑤). 후보 목록만 넘긴다.

### 외부 파일 위치 (리포 밖)
```
D1 export   C:\Users\user\OneDrive\바탕 화면\d1 consol.txt   (TSV, 1,200행 + 헤더)
학습지 PDF  C:\Users\user\OneDrive\바탕 화면\수학 문제\260711_학습집(도형의 닮음)\
            260711_도형의 닮음.pdf = set01 · (1)=set02 · … · (8)=set09
            260710_ 3개 = 07-10 판본(별개 450문항)
pdftotext   C:\Program Files\Git\mingw64\bin\pdftotext.exe
```

---

## 6. ★함정 (이 트랙에서 실제로 밟은 것)

```
검수가 인용하는 힌트는 요약이다        원문으로 취급하지 말 것. 좌표(배치·번호)로만 지목
1,200행을 옮겨 적지 말 것             육안 재입력본 금지. 파일로 받아 도구로 처리
-layout 은 긴 유형명을 두 줄로 접는다   '정답률' 이 쪼개져 긴 이름 문항만 골라 빠진다
                                     ★게이트가 없으면 141/150 을 성공으로 오인한다
잘못된 대응도 그럴듯한 목록을 만든다     한 칸 밀면 BASE 213건이 나온다. OK share 가드 필수
대각선이 이긴다고 지표를 채택하지 말 것  회전본이 지는지 보라
$pid 는 PowerShell 예약 변수다         변수명 충돌로 스크립트가 죽는다
[math]::Round($arr[$i,$j], 3) 는 파싱 실패  2차원 인덱스는 임시 변수에 담아서 넘길 것
PS 배열 리터럴의 정수 키 해시테이블      $h[1] 이 인덱스로 해석돼 표가 깨진다
.ps1 은 ASCII 전용                    한글·★·§ 전부 금지. 자기검사가 실행을 거부한다
                                     ★실제로 71바이트로 걸렸다. 규칙이 작동한다
전사본 metadata 를 좌표로 쓰지 말 것    bulk_batch_id 가 D1 배치보다 1 낮다. 정본은 D1
"내 쪽만 틀렸다"로 좁히지 말 것         1건으로 보고된 것이 전수 스캔하니 7건 전부였다
bash 루프에서 한글 경로는 반드시 인용    for f in $files 는 공백에서 쪼개진다. while IFS= read -r 로
Downloads 전사본은 D1 정본이 아니다     simpy-08 은 105/150 부분본이다. 행수부터 세라
카탈로그 전량 재직렬화 금지            response_formats 22종이 단일원소다. 왕복하면 스칼라가 된다
                                     ★엔트리 추가는 add_catalog_entries.ps1 로 텍스트 삽입
엔트리를 만들어도 대조는 안 변한다      라벨→슬롯 맵의 출처가 카탈로그가 아니라 문항은행이다
                                     ★"만들었으니 반영됐겠지"로 넘어가면 조용히 아무 일도 안 난다
카탈로그를 고치면 파생본을 세라        catalog_short 가 87에 정지해 있었다. 종수를 두 곳이 말한다
```

---

## 7. 다음 할 일 (판정 18차 §8)

```
✅종결
0  지목 4건 좌표 · 도구 채택 · 1,200건 결과 채택 (제출16 · 판정 18차)
1  빈 슬롯 4개(6·31·36·46) 생성 + 재대조 (제출18 · 판정 20차 승인)
   ★슬롯 3 은 생성하지 않았다(판정 18차 §5). 슬롯 1 은 현상 유지(판정 18차 §6)

검수 판정 대기 (Code탭 착수 불가)
2  파생 산출물 재생성 — catalog_short 가 87/87 로 정지 (제출18 §7)
   ★make_catalog_short.ps1 있음. 카탈로그 91 vs 축약본 87 불일치 상태
3  representation_types 오염 수정 착수 시점 · CHECK 9 신설 시점 (제출18 §6-3)
4  pos 표기 불일치 2곳 정정 (제출18 §5-3) ★조용히 고치지 말 것
5  BASE 104건 후보 판정 -> 재배정안
6  라벨 vs 풀이법 불일치율 — set06 150행 송부 완료. 검수 측정분
7  07-10 투입 여부 — 사용자 판단. distinct 79종 목록 송부 완료

원인 미규명
8  ★연쇄 밀림 45→47→48→50. 슬롯 46 가설 기각됨(제출18 §3-①). 새 가설 필요

이후
9  결손 접미사 72칸 중 문항이 요구한 것만 생성
10 difficulty 정정 (★GPT 전사분 한정, 재배정과 동시)

보류
11 원의 성질 11칸 재확인 (판정 16차 §4)
   ★같은 방법(이름 순서 정렬)의 적중률이 닮음에서 4/6 = 67% 였다
   ★단 빈 슬롯 4건은 학습지 라벨이 독립 증언해 4/4 바이트 동일이었다(제출17 §1-2)
12 통계 트랙 · 원의 성질 편입
```

---

## 8. 이 트랙에서 Code탭이 틀린 것 (반복 방지)

```
"87 위에서 게이트가 정상 동작 중"        405문항(34%)이 쪼개진 유형. C안 권고의 전제 반증
"결손 90종 이름은 어느 원본에도 없다"    72종 기계 도출 · 12종 순서 추론 가능
"mathflat 은 재료 부적합(2/53)"        비교 축이 틀렸다. 문항 라벨 vs mathflat = 66/67
"정식 126종 확보가 접미사의 정공법"      difficulty 필드가 이미 칸을 결정하고 있었다
"통계는 개념층까지 잘렸다"              개념층은 온전. 조립이 뒤쪽 11슬롯에 안 닿았을 뿐
"Read 도구가 PDF 를 직접 읽는다"        도구 설명만 보고 쓴 미검증 주장. pdftoppm 부재
"슬롯 1·3 은 미관측이므로 폐기"          슬롯 3은 07-10 판본에서 관측됐다
manifest +519/+150 의 커밋 귀속         이력을 최신→과거로 훑어 직전 커밋이 찍혔다
"지목 ID 가 simpy-05 번호 행과 맞는다"   증상만 적고 원인을 안 팠다. 실제 원인은 전사본 오프셋
                                     ★틀린 결론은 아니나 미완이었다. 전수 스캔으로 끝났어야 했다
```
열 건 다 **한 층만 보고 결론을 낸 것**이 원인이다. 이 프로젝트에서 반복되는 실패 유형이다.
★단 하나 잘 한 것: 자기 결과를 밀지 않고 **검수 측 입력 재확인을 요청한 것**이 두 라운드짜리
좌표 오류를 잡았다(판정 18차 §1). 상대가 입력 확인을 요청하면 먼저 확인한다 — 쌍방 원칙이다.

---

## 9. 검수 제출 이력 (Downloads, 리포 밖)

```
제출7   닮음 빈 슬롯 base 이름 추론안              순서정렬 · 슬롯6 SSS 반대
제출8   문항 라벨 증거                            빈 슬롯 교차확증 · 접미사=difficulty
제출9   manifest 해명 · D안 (a) 72칸               177 독립검증 · 회계 산술
제출10  개념층 증인                                제4 경로 · CHECK 7·8
제출11  통계 정정 · 경로 B 도구                     개념 증인은 하한만 말한다
제출12  경로 A PDF 추출 시험(set04)                 150/150 · 10구간은 재현 불가
제출13  9세트 1,350문항 추출                        빈 슬롯 4개 확증
제출14  대응 확정 · 1,200문항 배정 대조             BASE 103 · CELL 333
제출15  지목 4건 원인 규명 · 07-10 대조             좌표 불일치 · 슬롯 3 확증
제출16  전사본 batch_id +1 오프셋 규명              대응 4·5경로 · 잘림 6회째 · D1 무오염
        + set06 라벨 150행 · 07-10 distinct 79종 · BASE 103건 좌표 완비
제출17  빈 슬롯 4개 pos 단위 생성 목록              base 이름 2경로 바이트 동일 4/4
제출18  생성 완료 · 재대조                          NEWLABEL 35→21 · 연쇄밀림 가설 기각
        + pos 표준 목록 91행 · 오염 범위 1파일 1필드
```

★**필드 값 정의 문서**
```
ISSUE_type_name_source_values.v1.md   variant_bank | worksheet_label (판정 20차 §4-1)
```
검수는 리포 접근이 없다. 넘길 파일은 Downloads 복사 후 전달, 갱신본은 리비전 파일명.
