# ISSUE: 4단원 진단규칙 스키마 불일치 — 미소비 (2026-08-07 발견)

> 배선 조사 중 파생 발견. 검수 지시로 배선 하위항목 아닌 **별도 이슈**로 승격. 배선과 무관하게 **현재 진단 품질에 구멍**.

## 요약
`curated_diagnosis_rule` 상태의 **4단원 47개 진단규칙이 엔진 diagnose()에서 읽히지 않는다** — 규칙 스키마가 엔진이 기대하는 형태와 달라서. 규칙을 만들어 넣었으나 작동 안 하는 상태.

## 확정 근거 (코드·git)
| 스키마 | 트리거 필드 | 단원(규칙수) | 엔진 소비 |
|---|---|---|---|
| A (정본) | `trigger.error_tags_any` (중첩) | NE·QE·RC·LE 등 다수 | ✅ `math_weakness_engine.js:544` 읽음 |
| B | `trigger_error_tags` (최상위·`_any` 없음) | **QF(13)·TR(8)** | ❌ 미소비 |
| C | `if_observed_signals`·`then_diagnosis_concept_ids` | **GP(12)·PB(14)** | ❌ 미소비 |

- **[확인1] 규칙 정규화 없음**: `_normalizeAttempts`(:278)는 학생답안만 정규화. `trigger_error_tags`→`trigger.error_tags_any` 변환 코드 없음. line 544가 `rule.trigger.error_tags_any` 직접 참조.
- **[확인2] 타 소비처 없음**: `if_observed_signals`·`then_diagnosis_concept`·`trigger_error_tags`(word-boundary)를 읽는 런타임 코드 **0개**(assets·html·worker_skeleton 전수 grep). 엔진 밖 소비처도 리포엔 없음.
- **[확인3] git 이력 = 같은 날 다른 스키마(불일치 authoring)**:
  - GP·PB·QF: 2026-07-07 생성 후 **무수정**. 그런데 같은 날 생성된 QE는 **작동 스키마 A** 사용.
  - RC: 2026-07-06(작동 A). TR: 2026-07-20 "restore canonical bundle(PATCH276)"인데도 **미소비 스키마 B**.
  - → 깔끔한 마이그레이션(옛→새) 아님. **같은 시기에 단원마다 다른 스키마로 작성된 불일치.**

## 3갈래 판정 (검수 프레임)
| 갈래 | 근거 | 평가 |
|---|---|---|
| **① 미소비 = 버그/미완성** | 엔진이 스키마 A만 구현·4단원은 B/C·정규화 없음·타소비처 없음·같은날 불일치 authoring | **정황 강함(유력)** |
| ② 다른 경로로 소비 | 그 경로가 관측층 꽂힐 자리일 수 있음 | **근거 없음**(런타임 grep 0). 배포 Worker 내부는 미확인(리포 밖)이나 Worker는 진단규칙 아닌 추출 담당 |
| ③ 의도된 미사용 | 설계문서·주석 근거 필요 | 커밋 메시지 generic("Add … diagnosis data"), 미사용 의도 명시 없음. **근거 약함** |

⚠ 단정 금지(검수 지침). ①이 유력하나 배포 Worker·미열람 설계문서 여지 남김. 확정하려면: 배선 조사 항목1(Worker 프롬프트)·이 이슈용 설계문서/PATCH276 컨텍스트 추가 확인.

## 영향
- **GP(도형성질)·PB(확률)·QF(이차함수)·TR(삼각비) = error_tag 기반 진단이 현재 공백.** 이 단원 학생은 오답태그 기반 규칙 진단을 못 받음(개념점수·행동분석은 별개로 작동).
- ★역설: **이 4단원이 관측층(17축) 배선의 최대 수혜 대상**일 수 있음(진단 공백을 관측층이 메움).

## 권고 (배선보다 선행 가능)
1. **스키마 A로 통일**(가장 단순): B/C 규칙을 `trigger.error_tags_any` 형태로 변환하면 47규칙이 즉시 살아남. 단 규칙 내용(if_observed_signals·trigger_error_tags 값)이 엔진 태그어휘와 맞는지 확인 필요.
2. 또는 **엔진이 B/C 스키마도 읽도록 정규화 계층 추가**(규칙 파일 무변경).
3. 배선(관측층 신설)과 **별개 트랙**. 단 4단원 배선 설계 시 이 스키마 정리를 함께 고려.

## 상태
- 발견·기록만. 수정 실행 안 함(런타임 프로덕션 영향·검수/사용자 결정 대기).
- 관련: `B_wiring_investigation.v1.md`(배선 조사) · 배선 항목1(Worker) 미결.
