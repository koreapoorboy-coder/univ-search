# GPT 문항등록 명세 v1 — ★폐기(SUPERSEDED)

> **이 문서는 폐기됨.** 현행 = **[[B_gpt_item_registration_spec.v2.md]]** ((d) 유형 미배정·3단 구조).
> v1은 (d) 이전 초안이었다: GPT가 카탈로그(§2)로 `problem_type_id`를 직접 배정하는 설계.
> 카탈로그 층 조사([[B_catalog_layer_audit.v1.md]])에서 domain/subdomain 필드 부재·유형 종수 과다가 드러나,
> 검수 판정 (d)로 **GPT는 유형을 배정하지 않고 유형후보 힌트만 남기는 3단 구조**로 전환 → v2.
> ★같은 파일명 덮어쓰기로 인한 버전 혼동(2026-08-14 실발생) 방지 위해 v1은 스텁으로만 유지.
