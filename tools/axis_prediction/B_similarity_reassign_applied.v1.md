# 제출27 — 재배정 104건 적용 후 재대조

발신 Code탭 · 수신 검수 · 2026-08-21
회신 대상: **검수 판정 29차 §6-4/5 (적용 결과 재대조)**

---

## 0. 결과

```
BASE  104  ->  ★0
OK    718  ->  822
새로 생긴 BASE  ★0건
```

---

## 1. ① 104건이 전부 목표 유형으로 바뀌었는가 — 예

페이로드와 대조하지 않고 **D1 전후 export 를 전량 diff** 했습니다.
좌표 1,200개를 모두 비교한 것이라 "104건이 맞게 바뀌었다"와
"그 밖에는 아무것도 안 바뀌었다"를 동시에 봅니다.

```
before 1,200행   after 1,200행   사라진 좌표 0   생긴 좌표 0
바뀐 좌표        ★정확히 104
```
```
목표대로 바뀐 건           104 / 104
목표와 다른 값             0
★페이로드 밖인데 바뀐 것    0
★페이로드인데 안 바뀐 것    0
```
★ 사용자 화면이 보고한 `updated 104 · conflicts 0 · notfound 0` 와 **독립적으로 일치**합니다.
화면 자기보고를 그대로 받지 않고 데이터로 확인했습니다.

---

## 2. ② 전후 비교

```
                 재배정 전     재배정 후      차이
OK                    718          822       +104
CELL same family      346          346          0
BASE diff family      104        ★  0       -104
NEWLABEL               21           21          0
NOID                   11           11          0
compared            1,200        1,200          0
OK share            0.598        0.685      +0.087
라벨->슬롯             71           71          0
```

### 2-1. 행 단위 확인
```
후보 CSV   v3 482행  ->  v4 378행
v3 에만 있는 행  104   (전부 BASE)
v4 에만 있는 행  ★0
⇒ CELL 346 · NEWLABEL 21 · NOID 11 은 ★한 행도 안 바뀌었습니다
```

---

## 3. ③ 새로 생긴 BASE — ★0건

```
v4 에만 있는 행 0
```
BASE 가 0 이 된 것은 **104건 전부가 OK 로 넘어갔기 때문**입니다.
다른 판정에서 BASE 로 새로 떨어진 행은 없습니다.

★ 다만 이 결과의 성격을 정확히 적습니다.
```
BASE 가 0 인 것은 ★기대 슬롯이 판정 기준이자 재배정 목표였기 때문입니다
목표를 expected_slot 에서 도출했으므로, 적용하면 expected == assigned 가 됩니다
⇒ "라벨 기준으로는 어긋난 배정이 없다"는 뜻이지
   "모든 배정이 옳다"는 뜻이 아닙니다
```
남아 있는 것:
```
CELL 346   같은 계열 안에서 칸(pos)이 어긋난 것. 이번 범위 밖(판정 27차 §4-③)
NEWLABEL 21  카탈로그에 아직 슬롯이 없는 라벨
NOID 11      유형 배정 자체가 없는 문항
```

---

## 4. 되돌리기

```
B_simpy_rollback_payload_104.v1.json   적용 전 스냅샷. 그대로 올리면 원복됩니다
★유효합니다 — 적용 후 export 에서 104건이 전부 목표값이므로
  스냅샷의 "현재값"이 곧 "적용 전 값"으로 정확히 남아 있습니다
```

---

## 5. 산출

```
B_simpy_assignment_candidates.v4.csv   378행 (BASE 0 · CELL 346 · NEWLABEL 21 · NOID 11)
B_simpy_compare_report.v4.txt          재대조 리포트 원문
인계문 §3 갱신 (전/후/재배정후 3단 비교 + 적용 기록)
```

---

## 6. 커밋

```
(이번) 1개.  D1 은 사용자가 적용했고 Code탭은 쓰지 않았습니다. 리포에는 산출물만.
```

---

## 7. 남은 것

```
검수 판정 대기
  1  ★CELL 346건 처리 여부 — 재배정 트랙의 다음 단위
     판정 27차 §4-③ 에서 "이번 범위 밖"으로 두었습니다
  2  NEWLABEL 21건 — 슬롯이 없는 라벨. 슬롯 생성 대상인지 판정 필요
  3  NOID 11건 — 유형 배정 자체가 없는 문항
  4  지시서 개정 (제출26 실측 표 반영) — 원의 성질 2번째 전사 전
  5  circle-01 선언 문자열 정정본

사용자 조치
  6  push  ★미push 3건
  7  07-10 (1) -> 0710-02 · (2) -> 0710-03 전사

보류
  8  원의 성질 트랙 (판정 29차 지시로 계속 보류)
```
