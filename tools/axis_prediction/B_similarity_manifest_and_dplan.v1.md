# 제출9 — manifest 13192 해명 · 슬롯 확인 · 405 라벨 · D안 (a) 착수안 (2026-08-21)

> 수신 검수 · 발신 Code탭 · 판정 11차 §9 순서대로. **데이터 수정 없음.** 생성한 것은 목록 파일 1개(CSV)뿐이고 카탈로그는 건드리지 않았습니다.

---

## 0. 한 줄

**§6이 성공했습니다.** manifest 13192의 산술이 완전히 풀렸고, **"177 = 정의 수"가 독립 증거로 확인**됐습니다. 반면 §1·§2의 문항 확인은 **불가능**합니다 — 빈 슬롯 6개 전부 문항이 0건이기 때문이고, 이건 D안 (b)의 착수 조건 자체를 건드립니다.

---

## 1. ★★★ §6 manifest 13192 — 산술이 닫혔습니다

검수 지시: *"13192가 어느 시점의 어느 집합을 센 값인지 추적. 절단 이전 상태를 센 값이면 그 자체가 정식 카탈로그 크기의 독립 증거."*

### 값의 이력 (git 전수)
| 커밋 | 날짜 | manifest ptc | unit_count | 그 시점 실집계(json) | 차이 |
|---|---|---|---|---|---|
| `09d11e13` | 2026-07-09 | **12523** | 35 | **12523** | **0 — 정확했다** |
| `1c51ace1` | 2026-07-19 | 13042 | 39 | 12523 | +519 |
| `fd0d740e` | 2026-07-21 | **13192** | 39 | 12631 | +561 |
| (현재) | 2026-08-21 | 13192 | 39 | 12788 (index 39단원) | +404 |

**2026-07-09 시점에는 정확한 실집계였습니다.** 이후 두 번의 가산이 드리프트를 만들었습니다.

### ★+519의 정체 — 신규 4단원의 **선언 종수 합계**
`1c51ace1`에서 manifest `unit_count`가 35 → 39로 올랐습니다. 이때 편입 예정이던 4단원은:

```
M2_SIMILARITY_PYTHAGORAS   선언 177
M3_CIRCLE_PROPERTIES       선언 126
M3_STATISTICS              선언 108
M3_TRIGONOMETRIC_RATIO     선언 108
                        합계 519      ← manifest 가산액과 정확히 일치
```
★ 그 커밋에서 index에 실제로 추가된 것은 `M2_SIMILARITY_PYTHAGORAS` 하나이고, **가리키는 problem_types 파일은 그 시점에 존재하지 않았습니다**(`exists=False`). 즉 **파일보다 선언이 먼저 있었고, 선언 숫자는 177이었습니다.**

### ★대조군이 같은 배치 안에 있습니다
```
M3_TRIGONOMETRIC_RATIO  선언 108  →  현재 카탈로그 108 엔트리 / 최대ID 108  = 절단 없음
M2_SIMILARITY_PYTHAGORAS 선언 177 →  현재 87 / 최대ID 177                  = 절단
M3_CIRCLE_PROPERTIES     선언 126 →  현재 47 / 최대ID 126                   = 절단
M3_STATISTICS            선언 108 →  현재 23 / 최대ID 74                    = 절단
```
같은 날 같은 방식으로 선언된 4단원 중 **1개는 선언대로 채워졌고 3개는 절단**됐습니다. 선언 숫자가 실현 가능한 값이었다는 것을 대조군이 보여줍니다.

### ★★검산이 정확히 닫힙니다
```
13042 (= 12523 정확분 + 519 선언분)   ← 2026-07-19 시점의 "완전 빌드 기대 종수"
-12788 (현재 index 39단원 실집계)
= 254

절단 결손 합계 = (177-87) + (126-47) + (108-23)
              =   90     +    79    +    85     = 254      ★일치
```
⇒ **manifest는 절단 이전의 기대치를 담고 있었고, 현재 실집계와의 차이가 절단 결손과 정확히 같습니다.**
⇒ **"177 = 정의 수" 판정이 이름 추론·슬롯 산술과 무관한 제3의 증거(2026-07-19 회계 기록)로 독립 검증**됐습니다.

### +150 — 근거 없음
`fd0d740e`는 **`manifest.json` 한 파일만 바꿨습니다**(데이터 변경 0). 같은 구간에서 실집계는 +108(삼각비 카탈로그 착지)이었는데 manifest는 +150을 더했습니다.
```
150 = 이 프로젝트 학습지 1세트의 문항 수
가설: 종수 자리에 문항 수를 더한 단위 혼동 (§7 "억제 건수 ≠ 카탈로그 종수"와 같은 계열)
★가설로만 보고합니다. 커밋에 데이터 근거가 없어 확정할 수 없습니다.
```

### 판정 요청
```
13192 을 고칠 것인가
  권고 (A) 그대로 두고 별건 등재 + 주석  ← 판정 11차 §6 지시대로. 지금 상태
  참고 (B) 13042 로 정정하면 "완전 빌드 기대치"라는 의미가 살아난다
           단, 그러면 현재 실집계와의 차이 254 가 절단 지표가 된다
★Code탭은 (A) 유지 권고. 이 수치는 이제 증거물이라 손대면 증거가 사라집니다.
```

---

## 2. 🔴 §1 슬롯 6 문항 확인 — **불가능합니다**

검수 요구: *"슬롯 5(PT013·PT015) 문항과 mf#9 라벨 문항을 나란히 놓고 과제가 갈리는지 확인. 갈리지 않으면 슬롯 6은 불명으로 되돌립니다."*

```
mf#9 "두 삼각형이 닮음이기 위하여 필요한 조건" 라벨 문항 = 300건 중 0건
```
**비교 대상이 존재하지 않습니다.** 슬롯 6이 빈 이유가 바로 그것이기 때문입니다(제출8 §2).

### 슬롯 5에 실제로 있는 것 (전량)
```
PT013 [basic]    M2_SIMPY_150_S04_Q004   라벨 = 삼각형의 닮음 조건 (mf#8)
PT013 [basic]    M2_SIMPY_150_S04_Q005   라벨 = 삼각형의 닮음 조건 (mf#8)
PT015 [advanced] M2_SIMPY_150_S04_Q006   라벨 = 삼각형의 닮음 조건 (mf#8)
```
슬롯 5의 문항 3건은 **전부 mf#8** 라벨입니다. mf#9가 섞여 들어간 것이 아닙니다.

### 문항 대신 낼 수 있는 증거 (셋 다 간접)
1. **원본이 두 군을 나눴다** — mathflat이 mf#8과 mf#9를 별개 군으로 두고 topic_types도 다릅니다.
   ```
   mf#8 (4 topics) 주어진 삼각형의 닮음 조건 / 주어진 삼각형과 닮음인 삼각형 찾기 /
                   여러 삼각형 중 닮은 삼각형 찾기 / 이웃하거나 포함된 두 삼각형의 닮음
   mf#9 (1 topic)  두 삼각형이 닮음이기 위하여 필요한 조건
   ```
   과제 방향이 반대입니다 — mf#8은 **"닮은가/어느 조건인가"(판별)**, mf#9는 **"닮으려면 무엇이 더 필요한가"(조건 보충)**.
2. **우리 처방이 슬롯 5에 SSS·SAS·AA 판별을 이미 배정** (제출7 §4).
3. **변환 뱅크에도 없음** — set04/set09 variant bank 87종의 PT 번호 범위는 4~177이고, 빈 슬롯 6개에 해당하는 PT(1-3·7-9·16-18·91-93·106-108·136-138)는 **한 건도 없습니다.**

⇒ **판정 요청**: 검수 기준("갈리지 않으면 불명으로 되돌림")을 문자 그대로 적용하면 비교 자체가 불가능하므로 **슬롯 6은 §3의 '문항 미관측' 처리로 넘어갑니다.** 이름 후보는 유지하되 엔트리는 만들지 않는 쪽입니다.

---

## 3. 🔴🔴 §2 슬롯 1 — 그리고 이게 빈 슬롯 6개 전부에 해당합니다

검수 요구: *"mf#1 라벨 문항이 실제로 존재하는지. 없으면 '이름은 추론, 문항 미관측'으로 표시하고 엔트리 생성은 보류."*

```
mf#1 "닮은 도형에서 서로 대응하는 것" 라벨 문항 = 300건 중 0건
⇒ 슬롯 1 = 이름은 추론 · 문항 미관측 · 엔트리 생성 보류
```

★ **그런데 이 판정 기준은 슬롯 1만의 문제가 아닙니다.**

| 슬롯 | 후보 mf# | 라벨 문항 | variant bank | 상태 |
|---|---|---|---|---|
| 1 | mf#1 | **0건** | 없음 | 문항 미관측 |
| 3 | mf#5 | **0건** | 없음 | 문항 미관측 |
| 6 | mf#9 | **0건** | 없음 | 문항 미관측 |
| 31 | mf#35 | **0건** | 없음 | 문항 미관측 |
| 36 | mf#40 | **0건** | 없음 | 문항 미관측 |
| 46 | mf#50 | **0건** | 없음 | 문항 미관측 |

**빈 슬롯은 정의상 문항이 0건입니다.** 문항이 있었으면 슬롯이 비지 않았을 테니까요.

### ⇒ D안 (b)와 D안 원칙이 충돌합니다
```
D안 원칙       "90종을 미리 만들지 않는다. 문항이 요구할 때 그 자리 하나만 만든다"
판정 11차 §3   "(b) 빈 슬롯 18칸 생성" 채택
관측           빈 슬롯 6개를 요구하는 문항이 우리 표본(300)에 0건
```
**(b)를 지금 실행하면 "문항이 요구하지 않은 칸 18개를 미리 만드는 것"이 되어 D안 원칙을 위반합니다.**

### 판정 요청 (가장 중요)
```
(b) 빈 슬롯 18칸의 착수 조건을 다음 중에서 정해 주십시오

 B-1  보류    나머지 1,050문항(세트 01·02·03·05·06·07·08)에서 해당 라벨이
              하나라도 관측되면 그때 그 슬롯만 생성        ★Code탭 권고
 B-2  즉시    빈 슬롯이 정식 taxonomy의 일부임이 §1(manifest 519)로 확인됐으므로
              문항 관측과 무관하게 18칸을 생성
 B-3  부분    이름 확신이 높은 4개(3·31·36·46)만 생성, 1·6은 보류
```
Code탭은 **B-1** 권고입니다. §1에서 177이 정의 수임이 확인됐지만, 그것은 **"자리가 실재한다"**는 증거이지 **"지금 만들어야 한다"**는 근거는 아닙니다. 빈 칸은 처방·진단이 붙지 않은 상태로 남으면 원의 성질에서 겪은 "조용한 격하"를 재생산합니다.

---

## 4. §5 405문항의 `source_type_label` — 없습니다. 다만 원본에는 있습니다

검수 지시: *"있으면 기계 대조, 없으면 검수가 본문으로 판정. 어느 쪽인지가 작업량을 10배 가릅니다."*

### 답: GPT 전사분에는 **없습니다** — 명세가 금지하고 있습니다
```
tools/axis_prediction/B_gpt_item_registration_spec.v2.md §4
  "유형은 검수가 정한다. GPT는 판단 근거만 남긴다."
  "카탈로그 유형명을 쓰지 말 것. 자연어로 설명한다."
  source_note = "유형후보: <무엇을 묻는 문항인지> (<핵심 조건·수식 1개>)"
  problem_type_id 는 넣지 않음
```
⇒ **검수가 §0에서 본 `source_note`가 전부입니다.** 그 값은 자연어 설명이라 라벨과 기계 대조가 되지 않습니다. **의도적으로 그렇게 설계됐습니다**(유형 배정 정확도 90%라 GPT에 맡기지 않는다는 명세 §200).

### 그러나 `source_type_label`은 인제스트 파이프라인의 **불변량**입니다
```
전 단원 전 세트 전수: 25파일 3,364문항  ->  source_type_label 보유 3,364 (100%, 결손 0)
```
⇒ **인제스트를 거친 문항은 예외 없이 라벨을 가집니다.** set04·set09가 그 증거이고, 값은 mathflat 원본명과 66/67 축자 일치합니다(제출8).

### 라벨의 출처는 원본 PDF이고, 9세트가 같은 포맷입니다
```
data/sources/m2_similarity_pythagoras_worksheets.index.v1.json
  worksheet_count 9 · total_problem_count 1,350
  SET01~SET09 전부 동일 포맷 (문항 1-38p · 빠른답 39-40p · 해설 41-66p)
  그중 registry·item_bank 실재 = SET04 · SET09 뿐. 나머지 7세트는 선언만 있고 미인제스트
```
set04 coverage_manifest에는 **문항번호 구간 → 원본 유형 라벨** 매핑이 이미 10구간으로 남아 있습니다.
```
"001-017"  닮음의 뜻과 삼각형 닮음 조건
"018-033"  직각삼각형 닮음·평행선과 선분비
"034-053"  각의 이등분선과 사다리꼴 선분비   ...
```

### ⇒ 작업량 판단
```
경로 A (권고)  미인제스트 7세트의 PDF에서 "유형 헤딩 + 문항번호 구간"만 추출
               본문 전사 불필요. set04·set09에 완성된 전례가 있음
               산출 = 문항번호 -> 원본 라벨 매핑 -> 배정된 base 와 기계 대조
               필요한 것 = 원본 PDF 7개 (리포에 없음. 사용자 보유분)
경로 B         405문항 본문을 검수가 판정 (현행 가정)
```
★ **경로 A는 "본문 읽기"가 아니라 "목차 읽기"입니다.** 검수가 말한 10배 차이가 여기서 갈립니다.
**요청**: `260711_도형의 닮음.pdf` 외 6개(세트 01·02·03·05·06·07·08)를 주시면 유형 헤딩만 추출하겠습니다.

---

## 5. §4 difficulty 매핑 성격 — 문서화 완료. 리포에 이미 같은 경고가 있었습니다

검수 요구 문구를 그대로 D안 문서에 넣었습니다.
```
✕ difficulty 로 접미사를 예측할 수 있다
○ difficulty 와 pos 는 인제스트가 같은 값을 두 곳에 쓴 것이다
  → 기존 문항(인제스트 산출분)의 배치 규칙으로는 유효
  → ★신규 문항(GPT 전사분)에는 적용 금지
```

★ **그리고 이 경고는 리포에 이미 있었습니다.**
```
tools/axis_prediction/B_bulk_injection_json_spec.v2.md  difficulty 행
  enum ['basic','core','advanced','high'] 기본 'core'
  ★"source_item_bank 9종과 의도적 불일치 — 정렬/매핑 금지"(4값 고정)
```
전사분 difficulty는 **4값**이고 인제스트분은 **9종**이며, 명세가 **정렬·매핑을 명시적으로 금지**하고 있습니다. 검수 경고와 리포 명세가 독립적으로 같은 결론입니다. ⇒ 이 구분은 **확정**으로 두겠습니다.

---

## 6. §9-4 D안 (a) 착수안 — 72칸 목록 전량

파일: `tools/axis_prediction/B_simpy_missing_cells_72.v1.csv` (생성 완료, **카탈로그는 미수정**)

```
검산  점유 슬롯 53 x 3칸 = 159   -   보유 87   =   72        ✔
분포  pos0 개념·조건 판별 29 · pos1 비례식·정리 적용 14 · pos2 복합 계산·증명 29
컬럼  problem_type_id, slot, pos, base_name, suffix, derived_type_name, basis
근거  basis = mechanical_derivation (전건). 창작 0건.
      이름 = 해당 슬롯의 base + " - " + pos 고정 접미사. 예외 없음(제출8 §3에서 87/87 확인)
```
표본:
```
M2_SIMPY_PT006  slot 2  pos 2  평면도형의 닮음비 계산과 활용 - 복합 계산·증명
M2_SIMPY_PT010  slot 4  pos 0  회전체의 닮음비와 대응 길이 - 개념·조건 판별
M2_SIMPY_PT014  slot 5  pos 1  삼각형의 닮음 조건 판별 - 비례식·정리 적용
M2_SIMPY_PT020  slot 7  pos 1  SAS 닮음의 조건 적용 - 비례식·정리 적용
M2_SIMPY_PT024  slot 8  pos 2  AA 닮음의 조건 적용 - 복합 계산·증명
```

### ★(a)에도 같은 질문이 남습니다
72칸 중 **문항이 실제로 요구하는 칸이 몇 개인지**는 405문항 재배정(§4 경로 A/B) 전에는 모릅니다.
```
권고  72칸 목록은 "생성 승인 목록"이 아니라 "생성 가능 목록"으로 접수하십시오
      실제 생성은 405문항 재배정에서 그 칸을 요구하는 문항이 나온 것부터
      D안 원칙("문항이 요구할 때 그 자리 하나만")과 일치합니다
```
목록을 먼저 낸 이유는 **이름 도출이 기계적임을 검증받기 위해서**입니다. 72건 전부 창작이 아니라는 것이 확인되면, 이후 생성은 승인 없이 규칙 적용만으로 진행할 수 있습니다.

---

## 7. 부수 발견 — 카탈로그가 문항보다 먼저 있었습니다

set04 registry / coverage_manifest (2026-07-15, PATCH209):
```
new_canonical_type_count      0
matched_problem_count         150 / 150   (unmatched 0)
matched_canonical_type_count  87
mapping_status                "existing_canonical_type_matched"  (10구간 전부)
integration_mode              "기존 M2_SIMILARITY_PYTHAGORAS canonical 문항유형에 연결"
set09 도 동일: new_canonical_type_count 0
```
⇒ **87종은 set04/set09가 만든 것이 아닙니다.** 두 세트는 **이미 존재하던 canonical taxonomy에 연결만** 했고 새 유형을 하나도 만들지 않았습니다.
⇒ 우리가 지금까지 "87종은 문항에서 조립된 것"이라고 서술한 것은 **파일 생성 시점(2026-08-06 조립)** 이야기이고, **taxonomy 자체는 2026-07-15 이전부터 있었습니다.**
⇒ §1의 manifest 증거(2026-07-19 선언 177)와 시점이 맞물립니다.

★ 다만 **그 원본 taxonomy는 git 히스토리에 없습니다.** 결손 ID를 전 히스토리에서 검색한 결과:
```
git log --all -S"M2_SIMPY_PT006" / PT016 / PT091 / PT136  →  0건
PT001 만 1건 히트 = 문서에서 "PT001~087" 이라고 범위를 표기한 것(실데이터 아님)
전체 커밋 4,431개 대상
```

---

## 8. 회신 요청 정리

| # | 사안 | Code탭 권고 |
|---|---|---|
| **A** | **D안 (b) 착수 조건** — B-1 보류 / B-2 즉시 / B-3 부분 (§3) | **B-1** (문항 관측 시 그 슬롯만) |
| **B** | 슬롯 6 — 문항 비교 불가. 이름 후보 유지하되 §3 처리로 이관 | 이관 |
| **C** | 405 재배정 경로 — A(PDF 유형 헤딩 추출) / B(본문 판정) (§4) | **A**. PDF 7개 필요 |
| **D** | 72칸 목록의 성격 — 생성 승인 vs 생성 가능 (§6) | **생성 가능** |
| **E** | manifest 13192 — 그대로 두기 (§1) | 그대로. 증거물 |

---

## 9. 측정 재현

```
manifest 이력    git log --all -- public/math-weakness-engine/manifest.json  후 각 커밋 show
시점별 실집계    git ls-tree -r --name-only <sha> -- .../data/problem_types  후 각 파일 파싱
라벨 불변성      data/source_item_bank/*/*.json  items[].normalized_statement_features.source_type_label
빈 슬롯 문항 0   위 라벨 집합 vs mathflat 81군 이름 (공백 정규화 후 비교)
variant bank     data/type_variant_bank/m2_similarity_pythagoras/*.json  variants[].problem_type_id
72칸 목록        tools/axis_prediction/B_simpy_missing_cells_72.v1.csv
```
