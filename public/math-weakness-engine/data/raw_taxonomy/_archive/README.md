# _archive — 폐기/대체된 분류 파일 보관

여기 있는 파일은 **엔진에서 쓰지 않는다.** 빌더 글롭(`Get-ChildItem raw_taxonomy -Filter *.mathflat.v1.json`,
비재귀)이 이 하위폴더를 훑지 않으므로 재빌드·집계에 섞이지 않는다.

| 파일 | 사유 |
|---|---|
| `_pilot_h2_calculus1_differentiation.mathflat.v1.json` | 미적분Ⅰ 미분의 초기 시범추출본(`_status: pilot_partial`, 미분계수 한 구간 31개·세부유형 미수집). 정본 `h2_calculus1_differentiation.mathflat.v1.json`(79유형)으로 대체됨. 삭제 대신 이력 보관. |
