# GitHub 업로드 가이드

압축을 풀면 `public/math-weakness-engine/` 폴더가 나옵니다.
기존 GitHub 저장소의 `public` 폴더에 그대로 덮어쓰기 업로드하면 됩니다.

확인 경로:

```text
/math-weakness-engine/
```

추천 commit message:

```text
add middle school coordinate graph weakness diagnosis data
```
