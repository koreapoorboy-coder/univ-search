# GitHub 업로드 안내

## 업로드할 위치
압축을 풀면 `public/math-weakness-engine/` 폴더가 나옵니다.
기존 GitHub 저장소의 `public` 폴더 아래에 `math-weakness-engine` 폴더째 업로드하면 됩니다.

## Commit message 권장
`add math weakness engine seed data for middle school integer rational unit`

## 정상 확인
업로드 후 다음 주소가 열리면 정상입니다.

`https://<github-pages-domain>/math-weakness-engine/`

테스트 화면에서 샘플 데이터가 자동으로 들어가고, `진단 실행`을 누르면 취약 개념 점수와 중2 이후 연결 위험이 표시됩니다.
