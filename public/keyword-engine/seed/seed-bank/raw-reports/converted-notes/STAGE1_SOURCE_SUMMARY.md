# STAGE1_SOURCE_SUMMARY

원본 파일: `report_dataset_stage1_RPT001_005.xlsx`

## 변환 결과

원본 보고서 5개를 아래 seed로 변환했다.

| 원본 ID | seedName | 변환 방식 |
|---|---|---|
| RPT-001 | 시스템·데이터 기반 위험 예측형 | 원문 문장 복사가 아니라 `fusionType`, `corePattern`, `problemFrame`, `sectionBlueprint14`, `studentOutputBlueprint8`로 패턴화 |
| RPT-002 | 지속가능성·소재·디자인 해결형 | 원문 문장 복사가 아니라 `fusionType`, `corePattern`, `problemFrame`, `sectionBlueprint14`, `studentOutputBlueprint8`로 패턴화 |
| RPT-003 | 지구시스템·기후 데이터 해석형 | 원문 문장 복사가 아니라 `fusionType`, `corePattern`, `problemFrame`, `sectionBlueprint14`, `studentOutputBlueprint8`로 패턴화 |
| RPT-004 | 사회문제·정책 거버넌스 평가형 | 원문 문장 복사가 아니라 `fusionType`, `corePattern`, `problemFrame`, `sectionBlueprint14`, `studentOutputBlueprint8`로 패턴화 |
| RPT-005 | 공간 데이터·도시 최적화 모델링형 | 원문 문장 복사가 아니라 `fusionType`, `corePattern`, `problemFrame`, `sectionBlueprint14`, `studentOutputBlueprint8`로 패턴화 |

## 주의

이 원본은 seed 추출의 기준 파일로만 보관한다.
학생 화면이나 MINI prompt에 원문 전체를 그대로 넣지 않는다.
